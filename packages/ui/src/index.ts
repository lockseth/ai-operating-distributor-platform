// =============================================================================
// FlowSales UI Package
// Shared UI primitives. App-specific components live in apps/web/src/components.
// shadcn/ui components are installed directly in apps/web for now.
// This package will grow as cross-app component needs emerge.
// =============================================================================

export { Badge } from "./badge";
export { Button } from "./button";
export { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "./card";
export { LoadingSpinner } from "./loading-spinner";
export { StatusBadge } from "./status-badge";
